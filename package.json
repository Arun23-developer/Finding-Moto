{
  "name": "finding-moto-e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests for Finding Moto order flow",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:order": "playwright test tests/order-flow.spec.ts",
    "test:api": "playwright test tests/api-order.spec.ts",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.10.6"
  }
}
